#include <cs50.h>
#include <stdio.h>

int main(void)

{
    int x;
    do
    {
         x = get_int("Change owed: ");
    }
    while (n < 1 )
     // write conditions
     if (n / 25 = int)
     {
          printf("Minimum amount of coins: %i \n", n / 25)
     }
}
