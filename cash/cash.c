#include <cs50.h>
#include <stdio.h>

int main(void)

{
    int x;
    do
    {
         x = get_int("Change owed: ");
    }
    while (x < 1 );
     // write conditions
     if (x / 25 = int)
     {
          printf("Minimum amount of coins: %i \n", x / 25);
     }
}
