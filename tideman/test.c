#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x = 0;
    int y = 1;
    x = y;
    for (int i = 0; i < 3; i++)
        {
                x = i;
            }
        }
    printf("%i,%i",x,y);
}
